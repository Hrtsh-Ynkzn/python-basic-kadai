# kadai_013
# 商品を子入試手、消費税を加えた計算結果を返す関数を記述する。
# 第1引数に商品の金額、第２引数に消費税（10%）を設定する。

def Fee_contain_consumption_tax(price, consumption_tax):
    total = price * (1 + consumption_tax)
    return total

# 定義した関数を呼び出し、使用されている
Fee_contain_consumption_tax(1000, 0.1)
