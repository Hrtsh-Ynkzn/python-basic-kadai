# kadai_015

class Human:
    
    def __init__(self, name, age):    # コンストラクタ
        self.name = name
        self.age = age
    
    def printinfo(self):
        print("名前：" + str(self.name) + ", 年齢：" + str(self.age))

pr = Human("柚木園", 100)             # インスタンスの生成

# メソッドの利用
pr.printinfo()
